{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "claude-kit",
  "displayName": "Claude Kit",
  "version": "1.35.4",
  "description": "Multi-agent /workflow development pipeline (planner → plan-review → coder → code-review) with typed handoff contracts, lifecycle hooks, and MCP servers.",
  "author": { "name": "Dmitriy Mamykin", "url": "https://github.com/hex0xdeadbeef" },
  "repository": "https://github.com/hex0xdeadbeef/claude-kit",
  "keywords": ["workflow", "multi-agent", "pipeline", "tdd", "code-review", "planning"],
  "commands": ["./.claude/commands/"],
  "agents": [
    "./.claude/agents/code-researcher.md",
    "./.claude/agents/code-reviewer.md",
    "./.claude/agents/plan-reviewer.md",
    "./.claude/agents/verdict-recovery.md"
  ],
  "skills": "./.claude/skills/",
  "hooks": "./.claude/hooks/hooks.json",
  "mcpServers": "./.mcp.json",
  "userConfig": {
    "provision_settings_local": {
      "type": "boolean",
      "title": "Provision project settings.local.json",
      "description": "On first session in this project, merge the kit's opinionated env defaults (prompt-cache TTL, git co-author strip, auto-memory, strict-mode toggles) into .claude/settings.local.json — your existing values always win. Equivalent to what install.sh does for a project-scoped install. Off by default; effective the next session.",
      "default": false
    }
  }
}
